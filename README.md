# Elgato HW Monitor Plugin

Stream Deck plugin for hardware monitoring.

## Prerequisites

This project depends on [smctemp](https://github.com/narugit/smctemp) to read the CPU temperature.

Install it via Homebrew:

```bash
brew tap narugit/tap
brew install narugit/tap/smctemp
```
